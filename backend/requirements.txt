# Varinth Backend – Python Dependencies
# Python 3.11+

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# Pydantic v2 + settings (let pip resolve compatible version with supabase deps)
pydantic>=2.11.0
pydantic-settings>=2.7.0

# Supabase
supabase>=2.10.0

# HTTP client (NVIDIA API, async requests)
httpx>=0.26.0

# JWT decode (Supabase Auth verification)
python-jose[cryptography]>=3.3.0

# MCP server (stdio transport)
mcp>=1.2.0

# Security utils
python-dotenv>=1.0.0
cryptography>=42.0.0

# Structured logging
structlog>=24.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.24.0

# Utilities
python-multipart>=0.0.18
